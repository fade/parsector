image: alpine/edge
environment:
  mirror: git@github.com:fade/parsector.git
  GIT_SSH_COMMAND: ssh -o StrictHostKeyChecking=no
sources:
- https://github.com/fade/parsector
tasks:
- updatemirror: |
    cd parsector
    git push --mirror "$mirror"
